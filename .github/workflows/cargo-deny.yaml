name: Cargo deny

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions: {}

jobs:
  cargo-deny:
    name: Cargo deny
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
        with:
          persist-credentials: false

      # Reads deny.toml at the workspace root (advisories, licenses, bans, sources).
      - name: Run cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v2.0.20
